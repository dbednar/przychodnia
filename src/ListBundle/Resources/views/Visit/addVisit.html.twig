{% extends "::base.html.twig" %}
{% block title %}Umow Wizyte{% endblock %}
{% block body %}
    <div class="container">
        
        {% if message is defined %} 
            <p class="bg-success"> {{message}}   </p>
            <p><a role="button" class="btn btn-primary" href="{{path('visit')}}" >Powróć do strony wizyt</a></p> 
        {% endif %}
        {% if form is defined %} 
            <h2>Umów Wizytę </h2>
            {{ form_start(form) }}

            {{ form_end(form) }}
        {% endif %}
    </div>  


{% endblock %}  